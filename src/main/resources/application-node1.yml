spring:
  application:
    name: eureka-server

server:
  port: 8081



eureka:
  client:
    service-url:
      defaultZone: http://localhost:8082/eureka/,http://localhost:8083/eureka/
    register-with-eureka: true
    fetch-registry: true

  instance:
    hostname: node1
